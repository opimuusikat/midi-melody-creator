batch_id: "batch_plus_1200_key_coverage"
master_seed: 20260414
total_melodies: 1200

# Proportional split (approx 40/35/25), all >= number of keys in each tier config.
tier_distribution:
  tier1: 480
  tier2: 420
  tier3: 300

# v1 meters (6/8 dropped)
meter_weights:
  "4/4": 0.40
  "3/4": 0.35
  "2/4": 0.25

output_directory: "output/batch_plus_1200_key_coverage_v3"

quality_controls:
  max_generation_attempts: 20
  max_similarity_threshold: 0.20

# Ensure each key in each tier appears at least once.
ensure_key_coverage: true

